# Call the OpenAI embeddings API with exponential backoff on 429

Call the OpenAI embeddings API with exponential backoff on 429

## Usage

``` r
.openai_embed_texts(texts, api_key, verbose = FALSE)
```
