# Convert a node list + embed result into a data.frame for add_vertices

Convert a node list + embed result into a data.frame for add_vertices

## Usage

``` r
.nodes_to_vertex_df(nodes, embed_result)
```
