# Convert vertex attributes of a graph back into a minimal node list sufficient for edge-building.

Convert vertex attributes of a graph back into a minimal node list
sufficient for edge-building.

## Usage

``` r
.vertices_as_node_list(graph)
```
