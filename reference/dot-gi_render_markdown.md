# Render the Markdown document and trim to token budget

Render the Markdown document and trim to token budget

## Usage

``` r
.gi_render_markdown(
  pkg_name,
  build_date,
  proj_type,
  r_version,
  top_fns,
  conventions,
  pkg_inventory,
  max_tokens
)
```
